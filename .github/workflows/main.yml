name: Build and Deploy React App

on:
  push:
    branches:
      - master

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout code
        uses: actions/checkout@v4

      - name: 🛠️ Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 📦 Install dependencies
        run: npm install --force

      - name: ⚙️ Build React App (using env.dev)
        run: |
          echo "🛠️ Building with DEV environment..."
          npx env-cmd -f .env.dev npm run build

      - name: 🚀 Deploy to EC2
        env:
          EC2_HOST: ${{ secrets.EC2_HOST }}
          EC2_USER: ${{ secrets.EC2_USER }}
          EC2_SSH_KEY: ${{ secrets.EC2_SSH_KEY }}
        run: |
          # Write SSH key to a file and set permissions
          echo "$EC2_SSH_KEY" > ec2_key.pem
          chmod 600 ec2_key.pem

          # Test SSH connection first
          echo "🔌 Testing SSH connection to EC2..."
          ssh -i ec2_key.pem -o StrictHostKeyChecking=no -o ConnectTimeout=10 $EC2_USER@$EC2_HOST "echo '✅ SSH connection successful!'"
          if [ $? -ne 0 ]; then
            echo "❌ SSH connection failed. Exiting."
            exit 1
          fi

          # Copy build folder to EC2
          echo "📤 Copying build folder to EC2..."
          scp -i ec2_key.pem -o StrictHostKeyChecking=no -r dist $EC2_USER@$EC2_HOST:/tmp/dist

          # Deploy on EC2 in a single SSH session
          echo "🚀 Deploying React app on EC2..."
          ssh -i ec2_key.pem -o StrictHostKeyChecking=no $EC2_USER@$EC2_HOST <<EOF
          set -e  # Exit immediately if any command fails
          
          echo "🧹 Cleaning previous Nginx build..."
          sudo rm -rf /usr/share/nginx/html/theater-app
          
          echo "📁 Creating fresh directory..."
          sudo mkdir -p /usr/share/nginx/html/theater-app
          
          echo "📦 Moving new build files..."
          sudo cp -r /tmp/dist/* /usr/share/nginx/html/theater-app/
          
          echo "🧼 Cleaning up temp folder..."
          rm -rf /tmp/dist
          
          echo "🔁 Restarting Nginx..."
          sudo systemctl restart nginx
          
          echo "✅ Deployment complete!"

